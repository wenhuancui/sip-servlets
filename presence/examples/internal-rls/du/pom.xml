<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<packaging>pom</packaging>

	<parent>
		<artifactId>example-internal-rls-parent</artifactId>
		<groupId>org.mobicents.servers.sippresence</groupId>
		<version>5.1.1.GA-JBCP5</version>
	</parent>

	<artifactId>example-internal-rls-du</artifactId>

	<dependencies>
		<dependency>
			<artifactId>example-internal-rls-sbb</artifactId>
			<groupId>${pom.groupId}</groupId>
			<version>${pom.version}</version>
		</dependency>
	</dependencies>

	<!-- we will restrict the example du build and install to a non maven-release profile, to not install it when doing a release -->
	
	<profiles>
		<profile>
			<id>maven-release</id>
			<!-- empty profile -->
		</profile>
		<profile>
			<id>not-maven-release</id>
			<activation>
      			<activeByDefault>true</activeByDefault>
    		</activation>
			<build>
				<plugins>
					<plugin>
						<groupId>org.mobicents.tools</groupId>
						<artifactId>maven-du-plugin</artifactId>
						<extensions>true</extensions>
						<configuration />
						<executions>
							<execution>
								<phase>generate-resources</phase>
								<goals>
									<goal>copy-dependencies</goal>
									<goal>generate-descriptor</goal>
									<goal>generate-ant-management-script</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
			<artifactId>maven-assembly-plugin</artifactId>
			<configuration>
				<descriptors>
					<descriptor>src/main/assemble/descriptor.xml</descriptor>
				</descriptors>
			</configuration>
			<executions>
				<execution>
					<id>make-assembly</id>
					<phase>package</phase>
					<goals>
						<goal>single</goal>
						<goal>directory-single</goal>							
					</goals>
				</execution>
			</executions>
		</plugin>
					
				</plugins>
			</build>
		</profile>
	</profiles>
</project>