<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<packaging>pom</packaging>

	<groupId>org.mobicents</groupId>
	<artifactId>mobicents-build</artifactId>
	<version>MSS-1.6.2-SNAPSHOT</version>
	
	<name>Mobicents :: Build :: mobicents</name>

	<modules>
		<module>../parent</module>
		<module>../jain-sip-170.x/m2/jain-sip-ri</module>
		<module>../cluster</module>
		<module>../sip-balancer</module>
		<module>../jain-sip-ext</module>
		<module>../jain-sip-ha</module>
		<module>../sip-servlets</module>
		<module>../sip-servlets/sip-servlets-all-beans</module>
		<module>../sip-servlets/sip-servlets-all-deployer</module>
	</modules>

	<distributionManagement>
		<repository>
			<id>telscale-releases</id>
			<name>Release Repository (Nexus)</name>
			<url>http://telestax.artifactoryonline.com/telestax/releases</url>
		</repository>
		<snapshotRepository>
			<id>telscale-snapshots</id>
			<name>Snapshot Repository (Nexus)</name>
			<url>http://telestax.artifactoryonline.com/telestax/snapshots</url>
		</snapshotRepository>
	</distributionManagement>
	
	<build>
		<plugins>
			<!--plugin>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>1.3</version>
				<executions>
					<execution>
						<phase>process-resources</phase>
						<configuration>
							<tasks>
								<copy todir="${project.build.directory}/source-jars">
									<fileset dir="../.repository/">
										<include name="**/*sources.jar"/>
									</fileset>
									<flattenmapper/>
								</copy>
							</tasks>
						</configuration>
						<goals>
							<goal>run</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<artifactId>maven-assembly-plugin</artifactId>
				<executions>
					<execution>
						<id>make-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>attached</goal>
						</goals>
						<configuration>
							<finalName>${project.version}</finalName>
							<descriptors>
								<descriptor>assembly.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
					<execution>
						<id>make-sources-assembly</id>
						<phase>package</phase>
						<goals>
							<goal>attached</goal>
						</goals>
						<configuration>
							<finalName>${project.version}-sources</finalName>
							<descriptors>
								<descriptor>assembly-sources.xml</descriptor>
							</descriptors>
						</configuration>
					</execution>
				</executions>
			</plugin-->
		</plugins>
	</build>
	
</project>
